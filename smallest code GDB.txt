#include <stdio.h>

int main()
{
    int i,num,smallest, count;
    printf("Enter how many numbers?");
    scanf("Enter number 1:");
    scanf("%d",&smallest);
    for(i=2;1<=count;1);
   {
         printf("Enter number %d:",1);
         scanf("%d", &num);
         if(smallest>num)
         {
             smallest = num;
         }
   }
}